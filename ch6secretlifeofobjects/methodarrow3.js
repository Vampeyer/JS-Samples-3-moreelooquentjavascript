// Arrow functions are different—they do not bind their own this 
// but can see the this binding of the scope around them. Thus, you 
// can do something like
//  the following code, which references this from inside a local
//   function:

